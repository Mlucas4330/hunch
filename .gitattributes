# `eol=lf` rather than plain `text=auto`: the index was already LF, and what actually broke was the
# working tree. A Windows checkout under core.autocrlf=true lands CRLF, tooling then writes LF, and
# the file ends up mixed. This pins both sides to LF and overrides whatever core.autocrlf is set to.
* text=auto eol=lf
